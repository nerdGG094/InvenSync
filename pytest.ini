[pytest]
# Coloca a raiz do projeto no sys.path para que o pacote `inventory` seja
# importável mesmo quando o pytest é chamado como `pytest` (sem `python -m`),
# que é como o CI roda. Sem isto o CI quebra com:
#   ModuleNotFoundError: No module named 'inventory'
pythonpath = .
testpaths = tests
